// installed typescript using: sudo npm i -g typescript
// created tsconfig.json i.e configuration of typescript using: tsc --init
//  modified tsconfig.json for use of typescript features 

let age: number = 22;

if(age > 18) {
    age += 10;
    console.log(age);
}